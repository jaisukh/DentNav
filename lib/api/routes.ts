/** Same-origin API paths (Route Handlers under `app/api`). */
export const API_ROUTES = {
  questionnaire: "/api/questionnaire",
  analysis: "/api/analysis",
  authGoogle: "/api/auth/google",
} as const;
